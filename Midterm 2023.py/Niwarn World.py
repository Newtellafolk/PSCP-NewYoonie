"""Niwarn World"""
import math as m
def main(var_n, var_s, var_k):
    """ไปต่อหรือพอแค่นี้"""


def pikad(var_n, var_s, var_k):
    var_x = 2 + ((m.log*(var_n**2)/(2*var_n)*(m.log)*var_n))
    var_y = (m.sin(m.radians(30)+(2*var_s)**0.5)/(var_x*var_n))

def cal()
main(float(input()), float(input()), float(input()))
